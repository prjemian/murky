name: water_moccasin_test_environment

# ./water_moccasin_create.sh test.yml

channels:
  - conda-forge
  - defaults

dependencies:

  # --- Python core packages
  - python >=3.9
  - spec2nexus
  - pip

  - pip:
    - pyRestTable
    # comment will be ignored
